<% content_for(:meta_title) { @page.title } %>
<% content_for(:meta_description) { @page.description } %>
<h1><%= @page.title %></h1>
<div class="row">
  <div class="col-lg-12">
    <%= @page.body.html_safe %>
  </div>
</div>
